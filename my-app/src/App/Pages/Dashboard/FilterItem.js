import React, { Component } from 'react'; //import React Component

export class FilterItem extends Component {
    render() {
        return (
            <section>
            </section>
        );
    }
}